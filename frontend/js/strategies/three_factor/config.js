/**
 * 三因子策略 — 前端默认配置
 */
const THREE_FACTOR_DEFAULT_CONFIG = {
    initialCapital: 300000,
    positionRatio: 0.30,
    addRatio: 0.20,
    maxSinglePct: 0.50,
    maxTotalPct: 0.80,
    stopLossPct: -0.05,
    timeStopDays: 10,
    signalSellPct: 0.40,
    buyThreshold: 70,
    feeRate: 0.00025,
    tradeMode: 'manual',
};